{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "in=$(cat); printf '%s' \"$in\" | grep -q '\"stop_hook_active\": *true' && exit 0; out=$(dotnet test CavagnaDemo.Tests/CavagnaDemo.Tests.csproj --nologo 2>&1); [ $? -eq 0 ] && exit 0; { printf '%s\\n\\n=== TEST FALLITI — AZIONE RICHIESTA ===\\n' \"$out\"; printf 'NON applicare correzioni automaticamente. Descrivi il caso di test fallito riportato sopra, proponi una soluzione, poi passa in modalita piano e presentala per revisione con ExitPlanMode.\\n'; } 1>&2; exit 2",
            "timeout": 240,
            "statusMessage": "Eseguo dotnet test..."
          }
        ]
      }
    ]
  }
}
